# Specification: Project Scaffold

**Track ID:** project-scaffold_20260406
**Type:** Feature
**Created:** 2026-04-06
**Status:** Draft

## Summary

Ініціалізація Nuxt 4 проєкту з PrimeVue 4, Tailwind CSS 4, Supabase клієнтом, ESLint, Vitest, Playwright та базовим layout з BotC тематичним дизайном.

## Context

Blood on the Clocktower Tracker — ком'юніті сайт для трекінгу ігор BotC, статистики гравців та каталогу ролей. Це перший трек проєкту — фундамент для всіх наступних фіч.

## User Story

Як розробник, я хочу мати повністю налаштований проєкт з усіма залежностями, лінтером, тестами та базовим layout, щоб швидко почати розробку фіч.

## Acceptance Criteria

- [ ] `npm run dev` запускається без помилок
- [ ] `npm run lint:check` проходить
- [ ] `npm run typecheck` проходить
- [ ] `npm run test:run` проходить (хоча б 1 smoke test)
- [ ] `npm run test:e2e` проходить (хоча б 1 smoke test)
- [ ] PrimeVue Aura тема з BotC кольорами застосована
- [ ] Базовий layout (header з навігацією, footer) рендериться
- [ ] Supabase клієнт підключено (конфіг готовий)
- [ ] Tailwind з кастомними BotC токенами працює

## Dependencies

Немає — це перший трек, все з нуля.

## Out of Scope

- Supabase міграції (таблиці, RLS) — окремий трек
- Auth (Google OAuth, login/logout) — окремий трек
- Seed ролей з JSON — окремий трек
- Сторінки фіч (roles, games, players, leaderboard) — окремі треки
- Деплой на Vercel/Cloudflare — окремий трек

## Technical Notes

- Використати UI_DESIGN.md для кольорів, шрифтів та layout структури
- Використати IMPLEMENTATION_PLAN.md для структури проєкту та конфігів
- PrimeVue Aura theme з `definePreset` для BotC кастомізації
- Tailwind CSS 4 з `@theme inline` для BotC токенів
- @nuxt/eslint flat config (no semi, single quotes, no-console: error)
- Cinzel (Google Fonts) для заголовків, Inter для тексту

---

_Generated by Conductor. Review and edit as needed._
