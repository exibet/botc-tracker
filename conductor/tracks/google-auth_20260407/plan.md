# Implementation Plan: Google OAuth Authentication

**Track ID:** google-auth_20260407
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-07
**Status:** [x] Complete

## Overview

Implement auth in 3 phases: core composable, middleware, then UI integration into AppHeader. Uses `@nuxtjs/supabase` built-in auth with Google OAuth provider.

## Phase 1: Auth Composable

Core auth logic that all other phases depend on.

### Tasks

- [x] Task 1.1: Create `app/composables/useAuth.ts` — wraps `useSupabaseUser()` and `useSupabaseClient()`, loads profile from `profiles` table, exposes `user`, `profile`, `isAuthenticated`, `isAdmin`, `signInWithGoogle()`, `signOut()`
- [x] Task 1.2: Add auth types to `app/types/index.ts` (Profile already existed) — `Profile` interface matching profiles table schema
- [x] Task 1.3: Write unit tests for `useAuth` composable — test reactive state, isAdmin computed, signIn/signOut method calls

### Verification

- [ ] `npm run test:run` passes
- [ ] `npm run lint:check` passes
- [ ] `npm run typecheck` passes

## Phase 2: Route Middleware

Protect routes based on auth state and admin role.

### Tasks

- [x] Task 2.1: Create `app/middleware/auth.ts` — redirects unauthenticated users to `/` with toast message
- [x] Task 2.2: Create `app/middleware/admin.ts` — redirects non-admin users to `/` with toast message
- [x] Task 2.3: Write unit tests for middleware — test redirect logic for guest, player, admin

### Verification

- [ ] `npm run test:run` passes
- [ ] `npm run lint:check` passes
- [ ] Middleware can be applied to pages via `definePageMeta`

## Phase 3: Header UI Integration

Update AppHeader with sign-in button, avatar dropdown, and admin badge.

### Tasks

- [x] Task 3.1: Update `app/components/layout/AppHeader.vue` — add sign-in button (guest), avatar with dropdown menu (logged-in), admin badge
- [x] Task 3.2: Add PrimeVue `Toast` component to `app/app.vue` or layout for auth feedback messages
- [x] Task 3.3: Write component test for AppHeader — test guest view (sign-in button), player view (avatar + menu), admin view (badge)

### Verification

- [ ] `npm run test:run` passes
- [ ] `npm run lint:check` passes
- [ ] `npm run typecheck` passes
- [ ] Manual: Guest sees "Sign In" button
- [ ] Manual: After Google login, avatar and dropdown appear
- [ ] Manual: Admin badge visible for admin users
- [ ] Manual: Sign out returns to guest state

## Final Verification

- [ ] All acceptance criteria met
- [ ] All tests passing
- [ ] Lint and typecheck clean
- [ ] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
