# Implementation Plan: Player Self-Service

**Track ID:** player-self-service_20260407
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-07
**Status:** [x] Complete

## Overview

Add "Join Game" and "Edit My Entry" capabilities to the existing game detail page. Reuse existing components (RolePickerPanel, AlignmentTag) and composables (useGamePlayers). Create two new dialog components and integrate them into the game detail page.

## Phase 1: Join Game Dialog

Create the dialog component and integrate it into the game detail page.

### Tasks

- [x] Task 1.1: Create `JoinGameDialog.vue` — dialog with RolePickerPanel for role selection, alignment toggle (good/evil), and submit button. Props: `visible`, `roles`, `gameId`. Emits: `joined`.
- [x] Task 1.2: Integrate JoinGameDialog into `pages/games/[id]/index.vue` — show "Join Game" button when user is authenticated, not already in game. Wire dialog open/close and handle `joined` event to refresh players list.
- [x] Task 1.3: Wire submission logic — on join, call `useGamePlayers().add()` with current user as `player_id` and `added_by`, selected role as `starting_role_id`, selected alignment as `alignment_start`. Show toast on success/error.

### Verification

- [x] Task 1.4: Verify join flow end-to-end — logged-in player can open dialog, select role + alignment, submit, see themselves in the players table. Guest sees no button. Player already in game sees no button.

## Phase 2: Edit Own Entry

Allow players to edit their own entry from the game detail page.

### Tasks

- [x] Task 2.1: Create `EditEntryDialog.vue` — dialog pre-populated with player's current entry data. Fields: role (RolePickerPanel), alignment toggle, alive/dead toggle. Props: `visible`, `entry`, `roles`. Emits: `updated`.
- [x] Task 2.2: Add edit trigger to `GamePlayersTable.vue` — show an edit icon/button on the current user's row only. Emit `edit-entry` event with the player's entry data.
- [x] Task 2.3: Integrate EditEntryDialog into game detail page — handle `edit-entry` event from table, open dialog with entry data, on `updated` call `useGamePlayers().update()` and refresh.

### Verification

- [x] Task 2.4: Verify edit flow — player sees edit button only on own row, can change role/alignment/alive, changes persist and display correctly. Other players' rows have no edit button.

## Phase 3: Polish & Edge Cases

### Tasks

- [x] Task 3.1: Handle loading states — disable submit buttons while saving, show spinner in dialogs during API calls.
- [x] Task 3.2: Handle error states — toast messages for failed join/edit (e.g. duplicate entry, network error). Validate role is selected before allowing submit.
- [x] Task 3.3: Mobile responsiveness — ensure dialogs work well on mobile (bottom-sheet style per UI_DESIGN.md), touch-friendly targets.

### Verification

- [x] Task 3.4: Full integration test — verify all user roles (guest/player/admin) see correct UI, join and edit work on both desktop and mobile, error cases handled gracefully.

## Final Verification

- [x] All acceptance criteria met
- [x] No regressions in existing admin game edit flow
- [x] Responsive on mobile and desktop
- [x] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
